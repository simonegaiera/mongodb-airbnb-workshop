apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aibnb-workshop-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "aibnb-workshop-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aibnb-workshop-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
