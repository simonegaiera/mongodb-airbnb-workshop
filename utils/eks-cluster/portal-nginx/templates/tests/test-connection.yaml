apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "portal-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
