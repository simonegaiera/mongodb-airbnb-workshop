apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "portal-nginx.fullname" . }}-startup-script
  labels:
    {{- include "portal-nginx.labels" . | nindent 4 }}
data:
  startup.sh: |
{{ .Files.Get "files/startup.sh" | indent 4 }}
