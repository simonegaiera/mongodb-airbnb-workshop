apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "docs-nginx.fullname" . }}-startup-script
  labels:
    {{- include "docs-nginx.labels" . | nindent 4 }}
data:
  startup.sh: |
{{ .Files.Get "files/startup.sh" | indent 4 }}
