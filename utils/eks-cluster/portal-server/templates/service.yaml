apiVersion: v1
kind: Service
metadata:
  name: {{ include "portal-server.fullname" . }}
  labels:
    {{- include "portal-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "portal-server.selectorLabels" . | nindent 4 }}
