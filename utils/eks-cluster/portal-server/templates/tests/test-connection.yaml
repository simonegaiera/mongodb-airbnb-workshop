apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-server.fullname" . }}-test-connection"
  labels:
    {{- include "portal-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
